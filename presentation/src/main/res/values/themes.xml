<resources>

    <style name="Theme.SmokeCounter.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SmokeCounter.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SmokeCounter.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>